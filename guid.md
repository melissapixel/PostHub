┌─────────────────────────────────────────────────────────────┐
│                    КОМПЬЮТЕР                                │
│                                                             │
│  📁 Папка проекта (backend/)  ←───(синхронизация)───→  📦  │
│  (Вы меняете код здесь)                               КОНТЕЙНЕР
│                                                             │
│  🌐 Браузер                                                |
│  (http://localhost:8000)                                    │
│       │                                                     │
│       ▼                                                     │
│  ┌─────────────────────┐         ┌─────────────────────┐    │
│  │   🐘 PHP Контейнер  │ ──────→ │  🐘 PostgreSQL     │    │
│  │   (Порт 8000)       │  Запрос │  Контейнер          │    │
│  │                     │         │  (Порт 5432)        │    │
│  │  • Обработка запросов│         │                    │    │
│  │  • Логика API       │         │  • Таблица users    │    │
│  │  • PDO Driver       │ ←────── │  • Данные сохранены │    │
│  └─────────────────────┘  Ответ  └─────────────────────┘    │
│                            │                                │
│                            ▼                                │
│                     📁 Том Docker                           |
│                  (db_data - вечное хранение)                │
└─────────────────────────────────────────────────────────────┘


## Жизненный Цикл Данных
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  1. ИНИЦИА-  │     │  2. РАБОТА   │     │  3. ИЗМЕНЕ-  │
│  ЛИЗАЦИЯ     │     │  С ДАННЫМИ   │     │  НИЯ         │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                    │
       ▼                    ▼                    ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ docker-compose│    │ PHP делает   │     │ Новый файл   │
│ up (первый    │    │ SELECT/      │     │ миграции     │
│ запуск)       │    │ INSERT       │     │ 03_....sql   │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                    │
       ▼                    ▼                    ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ 01_init.sql  │     │ Данные в     │     │ Применяется  │
│ создает      │     │ таблице      │     │ вручную или  │
│ таблицы      │     │ users/posts  │     │ скриптом     │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                    │
       ▼                    ▼                    ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ Volume       │     │ JSON ответ   │     │ Структура БД │
│ сохраняет    │     │ в браузер    │     │ обновлена    │
│ данные       │     │              │     │              │
└──────────────┘     └──────────────┘     └──────────────┘