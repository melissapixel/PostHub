📄 docker-compose.yml
│
├─ Сервис "frontend":
│   └─ Взять папку ./frontend, собрать её и открыть порт 80
│
├─ Сервис "backend":
│   └─ Взять папку ./backend, запустить Node.js и открыть порт 5000
│
└─ Сервис "database":
    └─ Запустить базу данных PostgreSQL и сохранить данные в папку ./data